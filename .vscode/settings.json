{
  // Enables VSCode Tailwind support for clsx(), but doesn't work
  // indirectly through cn().
  "tailwindCSS.experimental.classRegex": [
    ["clsx\\(([^)]*)\\)", "(?:'|\"|`)([^']*)(?:'|\"|`)"]
  ]
}

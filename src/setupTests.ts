// The extend-expect import is no longer necessary as it was used in
// earlier versions of jest-dom to extend Jest's expect functionality,
// but the main import now covers that.
// ❌ import '@testing-library/jest-dom/extend-expect'
import '@testing-library/jest-dom'
